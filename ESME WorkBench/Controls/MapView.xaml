<UserControl x:Class="ESMEWorkbench.Controls.MapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:cinch="clr-namespace:Cinch;assembly=Cinch.WPF"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:Map="clr-namespace:ESMEWorkbench.ViewModels.Map"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:WpfDesktopEdition="clr-namespace:ThinkGeo.MapSuite.WpfDesktopEdition;assembly=WpfDesktopEdition"
             AllowDrop="True"
             d:DataContext="{d:DesignInstance Map:MapViewModel}"
             mc:Ignorable="d">
    <DockPanel LastChildFill="True">
        <WpfDesktopEdition:WpfMap x:Name="WpfMap" Cursor="{Binding Cursor}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="MouseLeftButtonUp">
                    <cinch:EventToCommandTrigger Command="{Binding MouseLeftButtonUpCommand}" />
                </i:EventTrigger>
                <i:EventTrigger EventName="MouseLeftButtonDown">
                    <cinch:EventToCommandTrigger Command="{Binding MouseLeftButtonDownCommand}" />
                </i:EventTrigger>
                <i:EventTrigger EventName="MouseRightButtonUp">
                    <cinch:EventToCommandTrigger Command="{Binding MouseRightButtonUpCommand}" />
                </i:EventTrigger>
                <i:EventTrigger EventName="MouseRightButtonDown">
                    <cinch:EventToCommandTrigger Command="{Binding MouseRightButtonDownCommand}" />
                </i:EventTrigger>
                <i:EventTrigger EventName="MouseMove">
                    <cinch:EventToCommandTrigger Command="{Binding MouseMoveCommand}" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </WpfDesktopEdition:WpfMap>
    </DockPanel>
</UserControl>