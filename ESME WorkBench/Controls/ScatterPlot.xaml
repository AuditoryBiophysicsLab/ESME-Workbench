<UserControl x:Class="ESMEWorkBench.Controls.ScatterPlot"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <!-- 
    <DockPanel>
        <DockPanel DockPanel.Dock="Left">
            <DockPanel>
                <TextBlock DockPanel.Dock="Bottom"
				           MinHeight="{Binding ElementName=RangeAxis, Path=ActualHeight}" />
                <controls:DataAxis x:Name="DepthAxis"
				                   AxisLabel="Depth (m)"
				                   AxisLocation="Right"
				                   StartValue="{Binding DepthMin}"
				                   EndValue="{Binding DepthMax}"
				                   TickValueFormat="0" />
            </DockPanel>
        </DockPanel>
        <DockPanel DockPanel.Dock="Bottom">
            <controls:DataAxis x:Name="SoundSpeedAxis"
			                   AxisLocation="Top"
			                   AxisLabel="Sound Speed (m/s)"
			                   StartValue="{Binding SpeedMin}"
			                   EndValue="{Binding SpeedMax}"
			                   TickValueFormat="0" />
        </DockPanel>
        <Canvas x:Name="PlotSurface" />
    </DockPanel>
    -->
</UserControl>
