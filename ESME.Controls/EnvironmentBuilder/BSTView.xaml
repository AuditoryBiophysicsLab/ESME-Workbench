<UserControl x:Class="ESME.Views.EnvironmentBuilder.BSTView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel Orientation="Vertical">

        <TextBlock Text="Database Resolution" Margin="2"/>
        <ComboBox ItemsSource="{Binding Resolutions}" 
                          SelectedItem="{Binding SelectedResolution}"                          
                          ToolTip="Choose a bathymetric resolution (in arc-minutes)">
        </ComboBox>
        <DockPanel LastChildFill="False">
            <Button Width="100"
			        Margin="10 0 0 0"
			        Content="Validate Database"
			        DockPanel.Dock="Right" />
        </DockPanel>
    </StackPanel>
</UserControl>
