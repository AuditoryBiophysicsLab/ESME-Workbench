<Window x:Class="ESME.Views.Environment.SoundSpeedProfileWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Environment="clr-namespace:ESME.Views.Environment"
        Title="{Binding WindowTitle}"
        Width="300"
        Height="300"
        WindowStyle="ToolWindow" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" mc:Ignorable="d" d:DataContext="{d:DesignInstance Environment:SoundSpeedProfileViewModel}">
    <DockPanel LastChildFill="True">
        <DockPanel Height="22" DockPanel.Dock="Bottom">
            <Button Content="CSV" Command="{Binding SaveToCSVCommand}" DockPanel.Dock="Right" HorizontalAlignment="Right" />
        </DockPanel>
        <Environment:SoundSpeedProfileView x:Name="SoundSpeedProfile" />
    </DockPanel>
</Window>
