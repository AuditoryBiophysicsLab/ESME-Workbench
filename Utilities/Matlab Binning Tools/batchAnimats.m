%batchAnimats 
%   batchAnimats(name) is a quick way to reprocess cumulative animat
%   exposure data generated by plotAnimatExposures (in turn, the direct
%   output of calculateStats. 
%
%   This function, when invoked in a directory containing the output of
%   plotAnimatExposures, graphs individual species histograms as well as
%   performs take estimates at a 100% population size with no resampling. 

function batchAnimats(name)
clear sels; 
clear spls;
load alldata;
graphAnimatStats(sels,spls);
resampleAndGraphAnimatStats(sels,spls,1,1,1,1,name);
end