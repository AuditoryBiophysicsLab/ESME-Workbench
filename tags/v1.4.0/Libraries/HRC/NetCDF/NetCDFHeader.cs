using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace HRC.NetCDF
{
#if false
    public class NetCDFHeader
    {
        private static char[] _magic = { 'C', 'D', 'F' };
        private char mVersion;
        private uint mRecordCount;
        private uint mDimensionCount;
        private uint mGlobalAttributeCount;
        private uint mAttributeCount;
        private uint mVariableCount;
    }
#endif
}
